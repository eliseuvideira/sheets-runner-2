components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer

  schemas:
    Error:
      type: object
      properties:
        message:
          type: string
          example: Internal server error
      required:
        - message

    TwitterMention:
      type: object
      properties:
      required:

    TwitterLike:
      type: object
      properties:
      required:

    TwitterRetweet:
      type: object
      properties:
      required:
